# Multi-AI Workflow CLI

> 版本 2.0.0 - 增强的多 AI 开发工作流，具备 CLI 控制和状态管理

一个强大的 Claude Code 技能，通过多个 AI 系统（Claude、Codex、Gemini）编排软件开发工作流。功能包括持久化状态管理、灵活配置和通过斜杠命令进行完整的工作流控制。

[English](README.md) | 简体中文

## 功能特性

- **标准项目结构** - 基于约定的目录布局（docs/、code/）
- **多 AI 编排** - 根据配置将任务委派给 Claude、Codex 或 Gemini
- **状态管理** - 基于 SQLite 的持久化，支持暂停/恢复功能
- **灵活配置** - 4 层优先级系统（CLI > 项目 > 全局 > 默认）
- **工作流控制** - 9 个斜杠命令实现完整的工作流管理
- **自动恢复** - 自动检测并恢复中断的工作流
- **错误恢复** - 优雅的错误处理，提供多种恢复选项

## 安装

### 前置要求

- Claude Code CLI
- SQLite3（macOS 预装）
- jq（用于 JSON 处理）

```bash
# 如需要，安装 jq
brew install jq

# 可选：安装 yq 用于 YAML 处理
brew install yq
```

### 设置

1. 将技能克隆或复制到您的 Claude 技能目录：

```bash
# 技能应位于：
~/.claude/skills/multi-ai-workflow-cli/
```

2. 初始化数据库：

```bash
~/.claude/skills/multi-ai-workflow-cli/scripts/db_manager.sh init
```

3. 验证安装：

```bash
# 检查技能是否可用
ls ~/.claude/skills/multi-ai-workflow-cli/

# 检查命令是否可用
ls ~/.claude/commands/workflow-*
```

## 快速开始

### 1. 初始化新项目

```bash
/workflow-init myproject
cd myproject
```

这将创建：
```
myproject/
├── docs/
│   ├── requirements.md
│   ├── design.md
│   └── reviews/
├── code/
├── .workflow/
├── README.md
├── .gitignore
└── .workflow-config.yaml
```

### 2. 启动工作流

```bash
/workflow-start
```

工作流将引导您完成：
1. **需求分析** - 生成 requirements.md 和 design.md
2. **代码实现** - 在 code/ 目录中生成完整代码
3. **代码审查** - 分析代码质量并生成审查报告
4. **优化** - 根据审查应用改进（可选）

### 3. 控制工作流

```bash
# 随时暂停
/workflow-pause

# 稍后恢复
/workflow-resume

# 检查状态
/workflow-status

# 查看所有工作流
/workflow-list
```

## 使用指南

### 项目结构

每个工作流项目都遵循以下标准结构：

```
<项目名称>/
├── docs/                        # 文档（约定）
│   ├── requirements.md          # 需求规格说明
│   ├── design.md                # 详细设计文档
│   └── reviews/                 # 代码审查报告
│       └── review-YYYYMMDD-HHMMSS.md
├── code/                        # 源代码（约定）
│   └── ...                      # 结构因技术栈而异
├── .workflow/                   # 工作流元数据（自动生成）
│   ├── config.yaml              # 项目特定设置
│   └── workflow-id.txt          # 当前工作流 ID
├── README.md                    # 项目文档
├── .gitignore                   # Git 忽略规则
└── .workflow-config.yaml        # 工作流配置
```

### 配置

#### 4 层优先级系统

配置按以下优先级解析（从高到低）：

1. **CLI 参数**（最高优先级）
   ```bash
   /workflow-start --step1=claude --step2=codex --step3=gemini
   ```

2. **项目配置**（.workflow-config.yaml）
   ```yaml
   ai:
     step1: claude
     step2: codex
     step3: gemini
   ```

3. **全局数据库配置**
   ```bash
   /workflow-config set step1_ai claude
   ```

4. **系统默认值**（最低优先级）
   - 步骤 1：claude
   - 步骤 2：codex
   - 步骤 3：gemini

#### 为每个步骤配置 AI

**全局配置（适用于所有项目）：**
```bash
/workflow-config set step1_ai claude
/workflow-config set step2_ai codex
/workflow-config set step3_ai gemini
```

**项目配置（编辑 .workflow-config.yaml）：**
```yaml
ai:
  step1: claude    # 需求分析
  step2: codex     # 代码实现
  step3: gemini    # 代码审查
```

**单次执行覆盖：**
```bash
/workflow-start --step1=claude --step2=claude --step3=codex
```

#### 查看配置

```bash
# 显示所有配置
/workflow-config show

# 显示有效配置（合并所有优先级后）
/workflow-config show effective

# 仅显示全局配置
/workflow-config show global

# 仅显示项目配置
/workflow-config show project
```

### 工作流命令

#### 初始化项目

```bash
# 在当前目录创建项目
/workflow-init myproject

# 在特定位置创建项目
/workflow-init myproject --path=/Users/me/projects
```

#### 启动工作流

```bash
# 使用默认配置启动
/workflow-start

# 使用自定义 AI 分配启动
/workflow-start --step1=claude --step2=codex --step3=gemini

# 所有步骤使用相同 AI
/workflow-start --ai=claude

# 在特定项目中启动
/workflow-start --project=/path/to/project
```

#### 暂停和恢复

```bash
# 暂停当前工作流
/workflow-pause

# 带备注暂停
/workflow-pause 需要审查需求

# 恢复最近暂停的工作流
/workflow-resume

# 恢复特定工作流
/workflow-resume a1b2c3d4-e5f6-7890-abcd-ef1234567890

# 恢复特定项目的工作流
/workflow-resume myproject
```

#### 检查状态

```bash
# 当前工作流状态
/workflow-status

# 特定工作流状态
/workflow-status a1b2c3d4-e5f6-7890-abcd-ef1234567890

# 项目状态
/workflow-status myproject
```

#### 列出工作流

```bash
# 列出所有工作流
/workflow-list

# 按项目过滤
/workflow-list --project=myproject

# 按状态过滤
/workflow-list --status=paused

# 限制结果数量
/workflow-list --limit=10
```

#### 回滚

```bash
# 将当前工作流回滚到步骤 2
/workflow-rollback 2

# 回滚特定工作流
/workflow-rollback 1 a1b2c3d4-e5f6-7890-abcd-ef1234567890
```

#### 清理

```bash
# 清理 7 天前完成的工作流
/workflow-clean

# 清理 30 天前的工作流
/workflow-clean --older-than=30

# 清理失败的工作流
/workflow-clean --status=failed

# 预览将要删除的内容
/workflow-clean --dry-run

# 清理特定项目
/workflow-clean --project=old-project --older-than=14
```

## 工作流步骤

### 步骤 1：需求分析

**默认 AI：** Claude
**输入：** 用户对需求的描述
**输出：**
- `docs/requirements.md` - 全面的需求规格说明
- `docs/design.md` - 详细的技术设计

**任务：**
- 分析用户需求
- 创建结构化需求文档
- 设计系统架构
- 定义技术方法
- 识别约束和依赖关系

### 步骤 2：代码实现

**默认 AI：** Codex
**输入：** 需求和设计文档
**输出：** `code/` 目录中的完整生产就绪代码

**任务：**
- 生成适合技术栈的项目结构
- 按需求实现所有功能
- 添加全面的注释
- 包含错误处理
- 创建测试
- 生成带使用说明的 README

### 步骤 3：代码审查

**默认 AI：** Gemini
**输入：** 生成的代码
**输出：** `docs/reviews/review-{timestamp}.md`

**任务：**
- 分析代码质量
- 检查安全漏洞
- 评估性能
- 审查测试覆盖率
- 评估可维护性
- 提供改进建议

### 步骤 4：优化（可选）

**AI：** 与步骤 2 相同
**输入：** 代码 + 审查反馈
**输出：** 优化后的代码

**任务：**
- 应用审查建议
- 修复识别的问题
- 改进代码质量
- 增强性能

## 高级用法

### 自定义项目模板

修改 `~/.claude/skills/multi-ai-workflow-cli/templates/` 中的模板：
- `README.template.md` - 项目 README 模板
- `gitignore.template` - Git 忽略规则
- `workflow-config.template.yaml` - 默认项目配置

### 自动恢复功能

在配置中启用/禁用自动恢复：

```yaml
# 在 .workflow-config.yaml 中
workflow:
  enable_auto_resume: true
```

或全局设置：
```bash
/workflow-config set enable_auto_resume true
```

启用后，技能会自动检测未完成的工作流并提示您恢复。

### 工作流期间的手动编辑

您可以暂停工作流，进行手动编辑，然后恢复：

```bash
# 启动工作流
/workflow-start

# 步骤 1 后暂停以进行手动编辑
/workflow-pause

# 手动编辑 docs/requirements.md
# ... 进行更改 ...

# 恢复工作流
/workflow-resume
```

### 处理多个项目

CLI 支持同时管理多个项目：

```bash
# 列出所有项目的工作流
/workflow-list

# 检查特定项目状态
/workflow-status myproject

# 恢复特定项目的工作流
/workflow-resume myproject
```

## 故障排除

### 数据库问题

```bash
# 重新初始化数据库（警告：丢失所有工作流历史记录）
rm ~/.claude/data/workflow-cli.db
~/.claude/skills/multi-ai-workflow-cli/scripts/db_manager.sh init
```

### 检查数据库状态

```bash
# 列出所有项目
~/.claude/skills/multi-ai-workflow-cli/scripts/db_manager.sh list_projects

# 列出所有工作流
~/.claude/skills/multi-ai-workflow-cli/scripts/db_manager.sh list_workflows

# 获取活动工作流
~/.claude/skills/multi-ai-workflow-cli/scripts/db_manager.sh get_active_workflows
```

### 外部 CLI 工具

如果 Codex 或 Gemini CLI 工具失败：

1. **验证安装：**
   ```bash
   which codex
   which gemini
   ```

2. **手动测试：**
   ```bash
   codex --version
   gemini --version
   ```

3. **检查配置：**
   - 确保工具在 PATH 中
   - 验证 API 密钥已配置
   - 如需要，检查代理设置

4. **使用替代 AI：**
   ```bash
   # 如果外部工具不可用，对所有步骤使用 Claude
   /workflow-start --ai=claude
   ```

### 工作流卡住或损坏

```bash
# 检查工作流状态
/workflow-status

# 如果损坏，您可以：
# 1. 回滚到较早步骤
/workflow-rollback 1

# 2. 或启动新工作流
/workflow-start

# 3. 或手动修复数据库
sqlite3 ~/.claude/data/workflow-cli.db
# UPDATE workflows SET status='failed' WHERE id='<workflow-id>';
```

## 架构

### 组件

```
┌─────────────────────────────────────────────────────────┐
│                     Claude（您）                         │
│                   协调者/编排者                          │
└────────────┬──────────────────────────────┬─────────────┘
             │                              │
   ┌─────────▼─────────┐        ┌──────────▼──────────┐
   │   配置管理器      │        │   状态管理          │
   │                   │        │                     │
   │  4 层优先级       │        │  SQLite 数据库      │
   └───────────────────┘        └─────────────────────┘
             │                              │
   ┌─────────▼──────────────────────────────▼─────────┐
   │              工作流引擎                            │
   │  步骤 1 → 步骤 2 → 步骤 3 → 步骤 4                │
   └──┬─────────┬────────────┬──────────────────────┬─┘
      │         │            │                      │
   ┌──▼──┐  ┌──▼───┐    ┌───▼────┐            ┌───▼────┐
   │Claude│  │Codex │    │ Gemini │            │脚本    │
   │      │  │ CLI  │    │  CLI   │            │        │
   └──────┘  └──────┘    └────────┘            └────────┘
```

### 数据库架构

```sql
projects          -- 项目注册表
workflows         -- 工作流实例
workflow_steps    -- 步骤执行记录
workflow_config   -- 全局配置
workflow_state    -- 暂停/恢复的执行上下文
```

### 文件结构

```
~/.claude/
├── skills/multi-ai-workflow-cli/
│   ├── SKILL.md                      # 主要工作流逻辑
│   ├── README.md                     # 本文件
│   ├── templates/                    # 项目模板
│   │   ├── README.template.md
│   │   ├── gitignore.template
│   │   └── workflow-config.template.yaml
│   ├── scripts/                      # 核心脚本
│   │   ├── init_project.sh           # 项目初始化
│   │   ├── db_manager.sh             # 数据库操作
│   │   ├── state_manager.sh          # 状态管理
│   │   ├── config_manager.sh         # 配置管理
│   │   ├── call_codex.sh             # Codex CLI 包装器
│   │   ├── call_gemini.sh            # Gemini CLI 包装器
│   │   └── schema.sql                # 数据库架构
│   └── references/                   # 文档
│       ├── document_templates.md
│       └── error_troubleshooting.md
│
├── commands/                         # 斜杠命令
│   ├── workflow-init.md
│   ├── workflow-start.md
│   ├── workflow-pause.md
│   ├── workflow-resume.md
│   ├── workflow-status.md
│   ├── workflow-config.md
│   ├── workflow-rollback.md
│   ├── workflow-list.md
│   └── workflow-clean.md
│
└── data/
    └── workflow-cli.db               # SQLite 数据库
```

## 示例

### 示例 1：简单的 Python 项目

```bash
# 初始化项目
/workflow-init python-calculator --path=~/projects
cd ~/projects/python-calculator

# 编辑需求
# （编辑 docs/requirements.md 以描述计算器需求）

# 启动工作流
/workflow-start

# 审查生成的需求
# 批准以继续

# 审查生成的代码
# 批准以继续审查

# 查看审查报告
# 选择优化或接受

# 完成！
```

### 示例 2：使用自定义 AI 的 Web 应用

```bash
# 初始化
/workflow-init my-webapp

# 对所有步骤使用 Claude（以便更快迭代）
/workflow-start --ai=claude

# 看到需求后，决定切换
/workflow-pause

# 使用正确的 AI 分配重新启动
/workflow-start --step1=claude --step2=codex --step3=gemini
```

### 示例 3：中断后恢复

```bash
# 启动工作流
/workflow-start

# ... 计算机崩溃或 Claude 会话结束 ...

# 稍后，重启 Claude 并导航到项目
cd myproject

# 技能自动检测暂停的工作流
# "发现 2025-01-15 的暂停工作流。恢复？"

# 是 -> 从您离开的地方继续
```

## 贡献

这是一个自定义技能。要修改：

1. 编辑 `SKILL.md` 以更改工作流逻辑
2. 编辑 `scripts/` 中的脚本以更改功能
3. 编辑 `~/.claude/commands/` 中的命令以更改命令行为

## 许可证

Apache 2.0

## 版本历史

### 2.0.0（2025-01-19）
- 使用 CLI 控制完全重写
- 添加 SQLite 状态管理
- 实现 4 层配置系统
- 添加 9 个斜杠命令
- 添加暂停/恢复功能
- 添加回滚能力
- 添加自动恢复检测

### 1.0.0（之前版本）
- 基本的多 AI 工作流
- 基于文件的状态跟踪
- 手动协调

## 相关资源

- [原始 multi-ai-workflow](https://github.com/fengxd1222/multi-ai-workflow)
- [Claude Code 文档](https://docs.claude.com/claude-code)
- [SQLite 文档](https://sqlite.org/docs.html)

---

**需要帮助？**

1. 检查工作流状态：`/workflow-status`
2. 查看配置：`/workflow-config show effective`
3. 列出所有工作流：`/workflow-list`
4. 阅读上面的故障排除部分
5. 检查工作流步骤记录中的日志

**编码愉快！🚀**
